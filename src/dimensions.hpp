#pragma once

#include <limits>

typedef unsigned short Task;
typedef unsigned Time;

const unsigned uint_inf = std::numeric_limits<unsigned int>::max();
const Time inf_time = std::numeric_limits<Time>::max();

const unsigned maxTasks = 512;
